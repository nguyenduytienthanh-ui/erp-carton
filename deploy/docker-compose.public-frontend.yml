services:
  frontend:
    build:
      context: ..
      dockerfile: frontend/Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://api.example.com/api/v1}
    restart: unless-stopped
    ports:
      - "${FRONTEND_PUBLIC_PORT:-8080}:80"
