import { Empty } from 'antd';

export default function AccountsPayableList() {
  return (
    <div style={{ padding: 40, textAlign: 'center' }}>
      <Empty description="Công nợ phải trả (A/P) chưa được triển khai" />
    </div>
  );
}
