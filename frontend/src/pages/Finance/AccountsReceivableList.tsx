import { Empty } from 'antd';

export default function AccountsReceivableList() {
  return (
    <div style={{ padding: 40, textAlign: 'center' }}>
      <Empty description="Công nợ phải thu (A/R) chưa được triển khai" />
    </div>
  );
}
