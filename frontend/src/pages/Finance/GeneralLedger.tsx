import { Empty } from 'antd';

export default function GeneralLedger() {
  return (
    <div style={{ padding: 40, textAlign: 'center' }}>
      <Empty description="Sổ cái tổng hợp chưa được triển khai" />
    </div>
  );
}
